# build_data.py : lire un classeur xlsx en pur Python. Écrire le JSON ne
# demande rien d'autre que la bibliothèque standard.
openpyxl==3.1.5
# build_arbre.py : relire le PDF du Manuel des GHM (images, traits, textes et
# leurs positions). L'empreinte de chaque symbole dépend du décodage de ses
# pixels : garder la version épinglée, ou revérifier SYMBOLES en la changeant.
pymupdf==1.28.2
